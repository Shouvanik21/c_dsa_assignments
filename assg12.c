#include<stdio.h>
#include<stdlib.h>

typedef struct node{
  int data;
  struct node *next;
}str;
str *ptr,*start='\0',*front=NULL,*rear=NULL;

void enqueue(int x){
  node=(nn*)malloc(sizeof(nn));
}

int dequeue(){
  int x;
}

int main(){
  return 0;
}
